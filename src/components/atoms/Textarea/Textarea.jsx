import styled from 'styled-components';

const Textarea = styled.textarea`
	width: 100%;
	height: 100px;
	padding: 4px 16px;
	white-space: wrap;
	outline: none;
	resize: none;
`;

export default Textarea;
